// import db from "../db.server";
export const loader = async ({ request }) => {
//   const session = await db.session.findFirst();
  return {"massage":" Congrats on  your sku app deployed !","name":"inventory app"};
};
